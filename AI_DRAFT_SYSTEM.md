# طبقة مسودات واتساب الذكية

## تحليل الأسلوب

- افتتاح دافئ ومباشر باسم العميل.
- إثبات قراءة الطلب بذكر المشكلة أو المجال تحديدًا.
- لغة سعودية طبيعية، مهنية، قصيرة، وبدون مبالغة تسويقية.
- فقرة واحدة لكل وظيفة: الفهم، الاقتراح، الخطوة التالية.
- لا يوجد تشخيص نهائي من وصف ناقص، ولا وعد بنتيجة غير مؤكدة.
- دعوة واحدة واضحة للفعل: حجز، سؤال محدد، أو جلسة مراجعة.
- إغلاق مختصر باسم خالد.

## النواة المنفذة

النواة مطابقة لمسار مكنة ERP: الواجهة لا تحمل مفتاح النموذج، وترسل طلبًا موثقًا إلى Cloudflare Worker. الـWorker يتحقق من جلسة Firebase وملكية الحساب، ثم يستدعي Cloudflare Workers AI باستخدام `@cf/zai-org/glm-4.7-flash`، وهو نموذج صيني متاح ضمن حصة Workers AI المجانية حسب الحساب. الناتج يعود كمسودة فقط ولا يُرسل تلقائيًا.

## دورة المسودة

1. فتح طلب العميل.
2. الضغط على «إنشاء مسودة ذكية».
3. النموذج يقرأ الاسم، نوع الخدمة، وصف الطلب، التصنيف، وملاحظات خالد.
4. خالد يعدل النص داخل المحرر.
5. الضغط على «تأكيد وتحديث رابط واتساب» يحفظ النص المعدل ويجعل رابط واتساب يستخدمه.

## حدود الأمان

- لا يتم تمرير رقم العميل للنموذج؛ الرقم يستخدم محليًا لبناء الرابط فقط.
- لا يتم إرسال الرسالة تلقائيًا؛ فتح الرابط يظل قرارًا بشريًا.
- عند تعطل خدمة الذكاء يظهر fallback محلي واضح بدل تعطيل لوحة الطلبات.
